import { defineComponent } from 'vue;
export default defineComponent({
  name: 'AFormItem',
  setup(props, { emit }) {
    return () => {
      return (
        <div>
          AFormItem
        </div>
      )
    }
  }  
})
